
spring:
  datasource:
    password: postgres
    url: jdbc:postgresql://localhost:5432/first_db
    username: postgres
  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
    show-sql: true